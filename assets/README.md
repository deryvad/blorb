# assets

Placeholder art lives here. Through the early phases the game generates its own
circle textures at runtime (see `BootScene`), so no external art is required yet.
Drop real sprites / audio in this folder when the visual pass (Phase 6) begins.
